#!/data/data/com.termux/files/usr/bin/bash
# Installs tailscale via upstream Android APK or CLI in Termux (we’ll wire logic)
set -euo pipefail
echo "[REYN-OS] Tailscale install stub (to be implemented)"
# TODO: detect if Tailscale app installed; else guide to F-Droid/Play
# TODO: optional: install 'ts' CLI in Termux via pkg if available
